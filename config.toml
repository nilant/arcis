merge_cells = true

# unique identifier (for merging)
instance_index = ["name"]

# columns order for instaces latex table
instance_order = ["nvertices", "narcs", "nedges", "nreq_arcs", "nreq_edges", "tot_services"]
instance_stats = []

# columns order for latex results table
models_order = []

# columns order for results latex table
algos_order = ["lb", "vidal_obj", "vidal_time", "rt_obj", "ls_obj", "gurobi_time", "total_time", "nroutes", "ls_time", "ls_iter", "best_time_ls", "best_iter_ls", "best_restart", "total_restart"]

# models
models = []

# algorithms
algos = ["bin/arcis"]

# timelimit
timelimit = 3600

# iterlimit
vidal_iterlimit = 1000
mls_iterlimit = 1

# memlimit
memlimit = 16

# threads (0 = all)
threads = 1

# seed
seed = 42
